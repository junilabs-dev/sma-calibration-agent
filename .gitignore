# Local TrueForge harness state (SQLite + sandbox roots). Regenerated by
# run_trueforge.ps1; deleting this folder is a full reset.
.trueforge-local/

# Local Python env + node deps
.venv/
node_modules/
__pycache__/
*.pyc

# Run artefacts -- rewritten on every agent run / seed_demo.py
progress_state.json
calibrated_material_card.json
.logs/
approval_pending.json
approval_decision.json
DEMO.md
